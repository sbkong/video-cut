<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VCut.Controls.VideoPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="*,Auto" Background="#0B0D14">
        <!-- 미리보기 화면 -->
        <Border Grid.Row="0" Background="Black" CornerRadius="6" Margin="0,0,0,8">
            <MediaPlayerElement
                x:Name="Player"
                AreTransportControlsEnabled="True"
                AutoPlay="False"
                Stretch="Uniform" />
        </Border>

        <!-- 프레임 단위 이동 + 시점 표시 -->
        <Grid Grid.Row="1" ColumnDefinitions="Auto,Auto,Auto,Auto,*,Auto" ColumnSpacing="6">
            <Button Grid.Column="0" x:Name="PrevKeyframeButton" Click="OnPrevKeyframe" ToolTipService.ToolTip="이전 키프레임 (Ctrl+←)">
                <FontIcon Glyph="&#xE892;" FontSize="12" />
            </Button>
            <Button Grid.Column="1" x:Name="PrevFrameButton" Click="OnPrevFrame" ToolTipService.ToolTip="이전 프레임 (←)">
                <FontIcon Glyph="&#xE76B;" FontSize="12" />
            </Button>
            <Button Grid.Column="2" x:Name="NextFrameButton" Click="OnNextFrame" ToolTipService.ToolTip="다음 프레임 (→)">
                <FontIcon Glyph="&#xE76C;" FontSize="12" />
            </Button>
            <Button Grid.Column="3" x:Name="NextKeyframeButton" Click="OnNextKeyframe" ToolTipService.ToolTip="다음 키프레임 (Ctrl+→)">
                <FontIcon Glyph="&#xE893;" FontSize="12" />
            </Button>
            <TextBlock Grid.Column="5" x:Name="PositionText"
                       VerticalAlignment="Center"
                       FontFamily="Consolas"
                       Text="00:00:00.000 / 00:00:00.000" />
        </Grid>
    </Grid>
</UserControl>
